<h1><%=@artist.name%></h1>
    <% @artist.songs.each do |song|%>
        <p><a href="#"><%=song.name%></a></p>
    <% end %>
    <% @artist.genres.each do |genre|%>
        <p><a href="#"><%=genre.name%></a></p>
   <% end %>